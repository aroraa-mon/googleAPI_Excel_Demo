{
  "name": "srm-foundation",
  "version": "1.0.0",
  "description": "googleAPI_excel demo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "commitLint": "npx --no-install commitlint --edit"
  },
  "pre-commit": [
    "commitLint"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^39.2.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.1",
    "@commitlint/config-conventional": "^12.1.1"
  }
}
